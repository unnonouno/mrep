language: python

python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

install:
  - pip install tornado
  - pip install coveralls

script:
  - coverage run --source=miura setup.py test

after_success:
  - coveralls
